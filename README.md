# portfolio site

## local dev

### required 

- node 10
- `elm`
- `elm-live` (npm, global)

### optional

- `elm-format`

### run

- `bash localdev.sh` - reloads on changes in elm files. Requires manual re-run when changing assets.

